let loaded=false;upload.onchange=e=>{const img=new Image();img.src=URL.createObjectURL(e.target.files[0]);img.onload=()=>{let c=originalCanvas,x=c.getContext('2d');c.width=img.width;c.height=img.height;x.drawImage(img,0,0);loaded=true;}};function convertImage(){if(!loaded){alert('Upload image');return;}grayCanvas.width=originalCanvas.width;grayCanvas.height=originalCanvas.height;let g=grayCanvas.getContext('2d');g.drawImage(originalCanvas,0,0);let d=g.getImageData(0,0,grayCanvas.width,grayCanvas.height);let a=d.data;for(let i=0;i<a.length;i+=4){let y=0.299*a[i]+0.587*a[i+1]+0.114*a[i+2];a[i]=a[i+1]=a[i+2]=y;}g.putImageData(d,0,0);}