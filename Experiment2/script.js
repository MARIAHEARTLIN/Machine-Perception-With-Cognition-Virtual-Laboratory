let ok=false;upload.onchange=e=>{let i=new Image();i.src=URL.createObjectURL(e.target.files[0]);i.onload=()=>{orig.width=i.width;orig.height=i.height;orig.getContext('2d').drawImage(i,0,0);ok=true;}};function equalize(){if(!ok){alert('Upload image');return;}out.width=orig.width;out.height=orig.height;let c=orig.getContext('2d').getImageData(0,0,orig.width,orig.height);let d=c.data,h=new Array(256).fill(0);for(let i=0;i<d.length;i+=4){let g=Math.round(.299*d[i]+.587*d[i+1]+.114*d[i+2]);d[i]=d[i+1]=d[i+2]=g;h[g]++;}let cdf=[],s=0;for(let i=0;i<256;i++){s+=h[i];cdf[i]=s;}let total=orig.width*orig.height;for(let i=0;i<d.length;i+=4){let g=d[i];let nv=Math.round((cdf[g]-cdf[0])/(total-cdf[0])*255);d[i]=d[i+1]=d[i+2]=Math.max(0,Math.min(255,nv));}out.getContext('2d').putImageData(c,0,0);}