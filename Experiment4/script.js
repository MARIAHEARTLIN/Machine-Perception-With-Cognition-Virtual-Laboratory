const upload=document.getElementById("upload"),canvas=document.getElementById("canvas"),ctx=canvas.getContext("2d");let img=new Image();upload.addEventListener("change",e=>{const r=new FileReader();r.onload=t=>{img.onload=()=>{canvas.width=img.width;canvas.height=img.height;ctx.drawImage(img,0,0)};img.src=t.target.result};r.readAsDataURL(e.target.files[0])});document.getElementById("threshold").addEventListener("input",function(){document.getElementById("value").innerText=this.value});function applyThreshold(){ctx.drawImage(img,0,0);let d=ctx.getImageData(0,0,canvas.width,canvas.height),a=d.data,th=parseInt(document.getElementById("threshold").value);for(let i=0;i<a.length;i+=4){let g=.299*a[i]+.587*a[i+1]+.114*a[i+2],v=g>th?255:0;a[i]=a[i+1]=a[i+2]=v}ctx.putImageData(d,0,0);}